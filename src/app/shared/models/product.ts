
export interface Product {
  id?: number;
  name: string; // manditory
  color?: string; // ? means this field is optional
  type?: string;
  price?: number;
  /*color: string
  type: string;
  price: number;
  createdDate: number;*/
  /*isComplete: boolean;*/
}


/*Service under shared means the connection to the controller in the backend.... */
